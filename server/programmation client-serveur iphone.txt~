 Ok, I finally got a connection using NSHost and NSStream.

Now my next problem is the following : send strings, longs, ints, dates, … into the stream. => how to convert to necessary uint8_t


SELECT id AS date_creation_fr FROM billets ORDER BY date_creation DESC LIMIT 0, 5

connection : 
	sécurisation
	envoie login, mdp (après sécurisation)
	ack_connection

deconnection : 
	demande déconnection
	déallocation de la mémoire.



toujours demande, réponse, … pas d'ack… sauf envoie de message (serveur) 3 essais

-> je me connecte par login, -> on se connecte on connection sécurisée, puis on récupère la db, des teams + un xml pour les messages. -> un xml pour les teams aussi… enfin 2 xml, ou du json, je sais pas. XML
